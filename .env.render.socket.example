# Render Socket Service (imengle-socket.onrender.com)
# One port only. Render injects PORT automatically.

# Core app
SPRING_PROFILES_ACTIVE=prod
WEB_APPLICATION_TYPE=none
SOCKETIO_ENABLED=true
SOCKETIO_HOST=0.0.0.0
# Do not set SOCKETIO_PORT on Render unless you need an override.
# App config already maps socketio.port -> PORT by default.

# Same backend data/secrets
MONGODB_URI=replace_with_your_mongodb_connection_string
JWT_SECRET=replace_with_strong_base64_secret

# CORS for frontend origin(s)
APP_CORS_ALLOWED_ORIGINS=https://imengle.netlify.app,https://www.imengle.netlify.app

# Optional
LOG_LEVEL=INFO
